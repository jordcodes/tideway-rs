//! Minimal auth module for Tideway starters.

mod provider;
mod routes;
{{#if has_database_feature}}
mod store;
{{#if is_sqlite_database}}
#[cfg(test)]
mod tests;
{{/if}}
{{/if}}

pub use provider::{SimpleAuthProvider, SimpleUser};
pub use routes::AuthModule;
{{#if has_database_feature}}
pub use store::{AppTokenIssuer, DbRefreshTokenStore, DbUserStore, InMemoryMfaTokenStore};
{{/if}}
