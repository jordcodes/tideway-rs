[package]
name = "{{project_name}}"
version = "0.1.0"
edition = "2021"

[dependencies]
tideway = { version = "0.7"{{#if has_tideway_features}}, features = [{{#each tideway_features}}"{{this}}"{{#unless @last}}, {{/unless}}{{/each}}]{{/if}} }
tokio = { version = "1.48", features = ["full"] }
axum = "0.7"
